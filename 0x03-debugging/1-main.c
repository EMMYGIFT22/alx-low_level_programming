#include <stdio.h>
/**
 * main - causes an infinite loop
 * Return: 0
 */
int main(void)
{
  /* the line causing an infinite loop */
  
  printf("infinite loop incoming :(\n");

  /* line causing an inifinite loop */

  /* line causing an infinite loop */
  /* line causing an infinite loop */
  /* line causing an infinite loop */
  /* line causing an infinite loop */

  printf("Infinite loop avoided! \\o/\n");

  return 0;
}
